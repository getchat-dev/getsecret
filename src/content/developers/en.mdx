---
title: "API"
lede: "Two endpoints power the whole flow — one stores an encrypted payload with a hashed access token, the other consumes that record exactly once in an atomic Lua transaction. All crypto happens in the browser; the server only sees ciphertext."
metaDescription: "Two endpoints, the request and response shapes, error codes, and rate limits — everything needed to build against Getsecret or run your own instance."
---

Reference shapes, error codes, and rate-limit details land here next.
