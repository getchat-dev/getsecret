APP_HOST="burnotes.markuper.com"
NODE_ENV=production
APP_ENV=prod
APP_PORT=3000
# Number of trusted reverse-proxy hops in front of the app (e.g., Traefik = 1).
# Set to 0 to disable header-based client-IP detection entirely.
TRUSTED_PROXY_HOPS=1
